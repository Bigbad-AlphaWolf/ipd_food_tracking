[build.environment]
  # Ensure environment variables are loaded during build
  NODE_ENV = "production"

# Redirect rules for Angular SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200